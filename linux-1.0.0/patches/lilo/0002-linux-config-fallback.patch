--- a/lilo.h
+++ b/lilo.h
@@ -18,8 +18,18 @@

 #ifdef HAS_BOOT_H
 #include <asm/boot.h>
-#else
-#include <linux/config.h>
+#endif
+
+/* Modern distros often omit kernel boot headers from userspace includes. */
+#ifndef DEF_INITSEG
+#define DEF_INITSEG 0x9000
+#define DEF_SYSSEG 0x1000
+#define DEF_SETUPSEG 0x9020
+#endif
+#ifndef NORMAL_VGA
+#define NORMAL_VGA 0xffff
+#define EXTENDED_VGA 0xfffe
+#define ASK_VGA 0xfffd
 #endif

 #define VERSION		20 /* Boot sector, map file, and chain loader format
