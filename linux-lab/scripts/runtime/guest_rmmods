#!/usr/bin/env bash
set -euo pipefail

RMMODS_CMD=${LINUX_LAB_GUEST_RMMODS_CMD:-bash scripts/rmmods}
SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
exec "$SCRIPT_DIR/connect" "bash" "-lc" "$RMMODS_CMD"
